# Python dependencies for ai-worker/pipelines/wan_inference.py.
#
# IMPORTANT: versions below are NOT pinned with confidence. This
# environment has no network access to check PyPI for current releases
# or Wan 2.2's actual tested-compatible versions. Treat these as
# starting points to verify against Wan's real model card / repo
# (search "Wan2.2" on Hugging Face or GitHub) before installing on real
# GPU infrastructure — see README.md.

torch>=2.1        # CUDA build required — install per pytorch.org's own
                   # instructions for your CUDA version, not from this
                   # file's line alone (torch's CUDA-specific wheels
                   # aren't resolvable from a plain requirements.txt)
diffusers          # VERIFY: exact package/extras Wan 2.2 actually needs
transformers
accelerate
pillow
safetensors
